// 사용자가 입력한 두 개의 숫자를 더해서 출력한다.
import java.util.Scanner; // Scanner 클래스 포함
// Scanner를 쓰기 위해선 어디에 있는지 선언해줘야함 import 구문은 불러와서 아래에 써야하기 때문에 제일 위에 씀


public class Add2 {
	// 메인 메소드에서부터 실행이 시작된다.
	public static void main(String args[]) {
		
		/* Scanner input;
		input = new Scanner(System.in); 변수 선언을 한문장으로 초기화, input은 다른 이름으로도 사용 가능 */
		Scanner input = new Scanner(System.in); // Scanner(이미 만들어놓은, 사용자가 입력할 수 있는 클래스, 객체)라는 타입에 input이라는 변수 선언
		/* new Scanner(System.in)까지가 인스턴스를 만들고 있는 것
		사용자로부터 입력을 받기 위해 Scanner(클래스=설계도)의 Instance(설계도대로 만들어놓은 건물)를 생성한다 */
		int x; // 첫 번째 숫자 저장
		int y; // 두 번째 숫자 저장
		int sum; // 합을 저장
		
		System.out.print("첫 번째 숫자를 입력하시오: "); // 입력 안내 출력
		x = input.nextInt(); // input 안에 있는 메소드 형태 호출하여 오른쪽에 있는 숫자를 왼쪽에 대입 연산으로 집어넣는다
		
		System.out.print("두 번째 숫자를 입력하시오: "); // 입력 안내 출력
		y = input.nextInt(); // 사용자로부터 두 번째 숫자를 읽는다.
		
		sum = x + y; // 두 개의 숫자를 더한다.
		
		System.out.println(sum); // 합을 출력한다.
		
	} // 메소드의 끝
	
} // 클래스의 끝